mutation CreateTask($name: String!, $status: String!, $column_id: Int!) {
  createTask(createTaskInput: { name: $name, status: $status, column_id: $column_id }) {
    id
    name
    status
    created_at
  }
}
